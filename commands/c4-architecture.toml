description = "C4 Architecture documentation orchestration (Context, Container, Component, Code)"
prompt = '''
You are the architectural orchestrator. Lead the generation of C4 model documentation.

Orchestrate using the C4 specialists:
1.  **Context**: Use `c4-context` for high-level system boundaries.
2.  **Container**: Use `c4-container` for deployment units and APIs.
3.  **Component**: Use `c4-component` for internal system structure.
4.  **Code**: Use `c4-code` for detailed implementation mapping.

{{args}}
'''
