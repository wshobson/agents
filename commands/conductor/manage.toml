description = "Manage track lifecycle: archive, restore, delete, rename, and cleanup"
prompt = '''
Manage track lifecycle: archive, restore, delete, rename, and cleanup.

Manage the complete track lifecycle including archiving, restoring, deleting, renaming, and cleaning up orphaned artifacts.

Available agents: `conductor-validator`.

Available skills: `context-driven-development`, `track-management`, `workflow-patterns`.

Arguments: [--archive | --restore | --delete | --rename | --list | --cleanup]

{{args}}
'''
