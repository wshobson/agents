# Stock Analysis Scripts - Python Dependencies

# Core dependencies (standard library - no installation needed)
# argparse, json, sys, datetime, typing, pathlib - built-in Python modules

# External dependencies
requests>=2.31.0          # For API calls (Tavily, Alpha Vantage, etc.)
pandas>=2.0.0             # For data manipulation and analysis
numpy>=1.24.0             # For numerical calculations
jinja2>=3.1.0             # For report template rendering

# Optional dependencies (for enhanced functionality)
yfinance>=0.2.0           # For Yahoo Finance data fetching (optional)
alpha-vantage>=2.3.0      # For Alpha Vantage API (optional)

# Installation:
# pip install -r requirements.txt
# Or for minimal installation:
# pip install requests pandas numpy jinja2
